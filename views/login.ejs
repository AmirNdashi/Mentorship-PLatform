<!DOCTYPE html>
<html lang="en">
<head>
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex justify-center items-center h-screen bg-gray-100">
    <form action="/login" method="POST" class="bg-white p-6 rounded-lg shadow-lg">
        <h2 class="text-xl font-bold mb-4">Login</h2>
        <% if (messages.error) { %>
            <p class="text-red-500"><%= messages.error %></p>
        <% } %>
        <input type="email" name="email" placeholder="Email" class="w-full p-2 mb-2 border rounded" required>
        <input type="password" name="password" placeholder="Password" class="w-full p-2 mb-2 border rounded" required>
        <button type="submit" class="bg-blue-500 text-white p-2 rounded">Login</button>
        <p class="mt-2">Don't have an account? <a href="/register" class="text-blue-500">Register</a></p>
    </form>
</body>
</html>
