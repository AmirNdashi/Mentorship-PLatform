<!DOCTYPE html>
<html lang="en">
<head>
    <title>Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">
    <nav class="bg-blue-500 text-white p-4">
        <div class="container mx-auto flex justify-between">
            <h1 class="text-xl font-bold">Mentorship Platform</h1>
            <a href="/logout" class="bg-red-500 px-3 py-1 rounded">Logout</a>
        </div>
    </nav>

    <div class="container mx-auto mt-6 p-4 bg-white shadow-lg rounded-lg">
        <h2 class="text-2xl font-bold">Welcome, <%= user.name %>!</h2>
        <p class="text-gray-600">You are logged in as a <%= user.role %>.</p>

        <div class="mt-4 flex gap-6">
            <!-- Profile Section -->
            <div class="w-1/3 bg-gray-200 p-4 rounded">
                <h3 class="text-xl font-bold">Profile</h3>
                <img src="/images/<%= user.profilePicture %>" alt="Profile Picture" class="w-32 h-32 rounded-full mt-2">
                <form action="/upload-profile" method="POST" enctype="multipart/form-data" class="mt-2">
                    <input type="file" name="profilePicture" class="border p-1">
                    <button type="submit" class="bg-blue-500 text-white p-2 rounded mt-2">Upload</button>
                </form>
            </div>

            <!-- Mentor/Mentee Specific Options -->
            <div class="w-2/3">
                <% if (user.role === "mentor") { %>
                    <h3 class="text-xl font-bold">Mentor Dashboard</h3>
                    <p class="text-gray-600">Manage your sessions and guide your mentees.</p>
                    <ul class="mt-2">
                        <li>📅 <a href="/sessions" class="text-blue-500">View Scheduled Sessions</a></li>
                        <li>📚 <a href="/resources" class="text-blue-500">Share Learning Materials</a></li>
                        <li>📝 <a href="/feedback" class="text-blue-500">Review Mentee Progress</a></li>
                    </ul>
                <% } else { %>
                    <h3 class="text-xl font-bold">Mentee Dashboard</h3>
                    <p class="text-gray-600">Track your progress and schedule sessions.</p>
                    <ul class="mt-2">
                        <li>📅 <a href="/book-session" class="text-blue-500">Book a Session</a></li>
                        <li>🎯 <a href="/goals" class="text-blue-500">Set Learning Goals</a></li>
                        <li>💡 <a href="/resources" class="text-blue-500">Access Learning Materials</a></li>
                    </ul>
                <% } %>
            </div>
        </div>
    </div>

</body>
</html>
